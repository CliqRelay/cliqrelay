lower
Converts a string or content to lowercase.

Example
#lower("ABC") \
#lower[*My Text*] \
#lower[already low]

Parameters
lower(
str
content
) → strcontent
text
str or content
Required
Positional
The text to convert to lowercase.
