General Symbols
Named general symbols.

For example, #sym.arrow produces the → symbol. Within math, these symbols can be used without the #sym. prefix.

The d in an integral’s dx can be written as $dif x$. Outside math formulas, dif can be accessed as math.dif.

Click on a symbol to copy it to the clipboard.
