# Data Commons

This is a library used to reuse models and utilities across multiple projects.

---

## NOTE

When creating any files in any of the folders inside of `src`, make sure to export them in their relevant `index.ts` file so that it can be imported and recognised in the other projects for use.

---
